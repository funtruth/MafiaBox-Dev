import {
    DEFAULT_EVENT_LISTENER,
    DEFAULT_ROLE_SETUP,
} from '../board/defaults'
import {
    defaultFieldMap,
    defaultFieldRepo,
    DEFAULT_GAME_CHOICE,
} from '../fields/defaults'
import {
    choiceMap,
    DEFAULT_ASSIGN,
    DEFAULT_LOGIC,
    DEFAULT_VAR_ID,
    rssMap,
    triggerNewVars,
    VAR_DEFAULTS,
} from '../logic/defaults'

export {
    choiceMap,
    defaultFieldMap,
    defaultFieldRepo,
    DEFAULT_ASSIGN,
    DEFAULT_EVENT_LISTENER,
    DEFAULT_GAME_CHOICE,
    DEFAULT_LOGIC,
    DEFAULT_ROLE_SETUP,
    DEFAULT_VAR_ID,
    rssMap,
    triggerNewVars,
    VAR_DEFAULTS,
}