import React from 'react'

import {
    Body,
    Row,
} from '../../../components/Common'

export default function ModeRolePicker(props) {
    return (
        <Body
            className="--basic-slide"
            sizes={['xs', 'xs']}
        >
            <Row
                bg="charcoal"
            ></Row>
        </Body>
    )
}